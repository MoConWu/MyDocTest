package darkMoonUI.controls {
/*import darkMoonUI.assets.colors.bgColorArr;
import darkMoonUI.assets.functions.fill;
import flash.events.NativeWindowBoundsEvent;
import flash.events.Event;
import flash.display.Shape;*/

import darkMoonUI.assets.DarkMoonUIControl;

/**
 * @private
 * @author moconwu
 */
public class TabPanel extends DarkMoonUIControl {
	/*private var
	_bg : Shape = new Shape(),
	_tabHead : Shape = new Shape(),
	_headSelMC : Shape = new Shape(),
	_bgSelMC : Shape = new Shape(),
	_tabHeadMask : Shape = new Shape();*/

	/*public function TabPanel() {
		super();
		_type = "tabPanel";

		fill(_bg.graphics,10,10,bgColorArr[3]);

		addEventListener(Event.ADDED_TO_STAGE, ADDED);
	}

	private function ADDED(evt : Event) : void {
		stage.nativeWindow.addEventListener(NativeWindowBoundsEvent.RESIZE, RESIZE);
		stage.nativeWindow.addEventListener(NativeWindowBoundsEvent.RESIZING, RESIZE);
	}

	private function RESIZE(evt : NativeWindowBoundsEvent) : void {
		size = [stage.stageWidth, stage.stageHeight];
	}*/
}
}